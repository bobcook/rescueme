<%= form_for(@kpi_notification) do |f| %>
  <% if @kpi_notification.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@kpi_notification.errors.count, "error") %> prohibited this kpi_notification from being saved:</h2>

      <ul>
      <% @kpi_notification.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title, :style => 'width:100%;' %>
  </div>
  <div class="field">
    <%= f.label :value %><br>
    <%= f.text_field :value, :style => 'width:100%;' %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description, :style => 'width:100%;' %>
  </div>
  <div class="actions">
    <%= f.button :submit, :class => 'btn btn-md btn-primary' %>
  </div>
<% end %>
